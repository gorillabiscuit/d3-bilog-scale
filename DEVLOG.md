# DEVLOG — d3-outlier-graphs

Decision log for the article. Timestamped one-liners only.

---

- **[14:30]** Rejected uniform hatch lines across entire log tail: it recreates the bunched-lines look at 45° and communicates nothing new about per-region compression.
- **[14:35]** Redesigned hatch texture to per-band density encoding: each equal-dollar-step section gets its own SVG `<pattern>` whose line spacing scales with that band's pixel width relative to the widest band in the tail — widest band = sparsest hatch (8px), narrower = denser, sub-1px = solid fill.
- **[14:40]** Key insight that forced relative normalisation: absolute normalisation against linearW caused all bands to fall below the solid-fill threshold on datasets with highly-compressed tails (NYC property data: first right-tail band only 11.5px vs 113px linear window, giving 1.2px spacing before any pattern renders).

---

- **[session-start]** Defined the core problem: 200 small business loans where 84% cluster under $10K but 10 outliers reach $1.2M, making a naive linear scale useless for the majority.
- **[research]** Chose to research 21 techniques across four categories (scale-transform, visual-encoding, interaction, layout) before committing to any implementation.
- **[research]** Rejected `d3-scale-break` plugin as a dependency: only 4 GitHub stars, pre-1.0, last pushed January 2023 — effectively unmaintained.
- **[research]** Noted that `d3.density()` and `d3.kde()` do not exist in D3 v7 — violin plots require a hand-rolled kernel density estimator; the outline.yaml was incorrect on this point.
- **[research]** Noted that `d3.facet()` does not exist in D3 v7 — it is an Observable Plot concept; raw D3 small multiples use `d3.group()` + per-tier SVGs.
- **[research]** Cleveland's strongest recommendation for skewed data: log scale first, then full-panel break (small multiples) if log won't work — explicitly prefers this over a slashed/partial axis break.
- **[research]** ECDF identified as the strongest literature-endorsed technique for reading exact outlier values (Cleveland, Wilke, Brooker) but currently underused in D3 tutorials.
- **[research]** Symlog (`d3.scaleSymlog`) identified as the key differentiator among scale transforms: the only one that handles $0 and negative values natively, addressing Cleveland's "when log won't work" condition.
- **[research]** d3-annotation (Susie Lu, 762★) confirmed as the canonical D3 approach for Tufte-style in-place outlier labeling; built for d3-v4 but works with v7, low-maintenance.
- **[research]** Focus+Context (`@d3/focus-context`) confirmed as the strongest single fit for this dataset: context strip keeps $1.2M loan to-scale while focus pane decompresses the $0–$10K mass.
- **[17:49]** Scale core implemented: three-region piecewise (log-linear-log) with IQR and percentile breakpoint methods. All 28 tests passing: monotonicity, invertibility, boundary continuity, edge cases, graceful degradation, D3 compatibility.
- **[15:00]** Project scaffolded: Vite + D3 v7 + Vitest. Three-chart layout with dataset selector. Five test dataset generators with seeded PRNG (mulberry32). Scale architecture split into src/scale/ (pure math) and src/chart/ (DOM/D3).
- **[18:35]** Applied fmt() formatter globally to all chart x-axes — linear scale was showing raw numbers (20,000,000) because d3.scaleLinear also has a .tickFormat method, making the adaptive-vs-native check unreliable; switched to checking for .breakpointMethod instead.
- **[18:35]** Set MIN_GAP=72px for adaptive scale tick post-filter — at 1150px inner width the 48px threshold still allowed 17 ticks in the dense cluster region; 72px brings it down to ~11.
- **[18:47]** D3 idiom audit: found 7 issues — removed unused pointer import, switched SVG to viewBox+CSS sizing, replaced container.id-based clipPath ids with an incrementing counter, added ResizeObserver with 120ms debounce, added scale.type='adaptive' to replace duck-type check on breakpointMethod, fixed yPad fallback for small y values.
- **[19:02]** Rewrote chart layer to Observable cell pattern: createChart() returns svg.node() via d3.create(), SVG-internal tooltip using pointer(event, g.node()), d3-format replaces hand-rolled formatter, per-dataset xFormat/yFormat specifiers in loaders, replaceChildren() in index.js.
- **[19:15]** Added x-axis gridlines to adaptive chart using tickValues(xScale.ticks()) + tickSize(-innerH) — gridlines share exact positions with axis labels; the log-spaced crowding in the tails vs even linear spacing in the middle is now immediately visible.
- **19:16** Replaced flat gridlines with semi-log paper ruling: lines at every integer multiple within each decade (1×–9×) mapped through a log sub-scale, producing visible bunching near each power of 10 — compression encoded as physical density rather than labelling.
- **[20:51]** Added distribution detection (skewness ratio test on log-transformed values) — USGS now auto-selects log scale and disables slider; NYC/SBA stay on piecewise
- **[21:22]** Added KDE and mixture-model window detection as alternatives to quantile — dropdown lets user compare all three on the same data
- **[23:15]** Fixed pixel-boundary jump: quantile method now derives r1/r2 from slider fraction directly; KDE/mixture use smoothFraction() (linear interpolation through sorted values) instead of discrete count
- **[23:19]** Fixed runaway tick-line loop: break as soon as consecutive ticks are < 2px apart (power scale compresses monotonically, so first gap below threshold means all further gaps will be too)
- **[23:24]** Fixed inverted scale on KDE/Mixture/Quantile: 0.001×range clamp in windowQuantile was larger than the actual 25th percentile on USGS data (250k > 4), swapped xLo and xHi silently — replaced with 1e-9 epsilon
- **[23:29]** Added test suite for window functions: xLo<xHi invariant, monotonicity, bounds, and edge cases across NARROW/WIDE/BIMODAL dataset shapes — caught two real bugs (KDE/Mixture unbounded output, fp round-trip)
- **[09:20]** Fixed slider jumps: replaced smoothFraction (empirical CDF, jumps at data clusters) with hybrid — quantile uses 0.5-slider/2 (linear, data-free), KDE/Mixture use logFraction (log-space position of xLo/xHi) — all methods now at step ratio 1.0 across all dataset shapes
- **[09:25]** Fixed tick-line runaway on extreme-range datasets: replaced manual domain-step while-loop with scalePow.ticks(n) — the same algorithm d3-axis uses; budget proportional to each tail's pixel fraction caps total lines at ~12
- **[09:47]** Removed KDE and Mixture model from UI dropdown — both methods placed the linear region off-centre (left/right instead of middle) which made the chart harder to read than quantile, and drag handles make method selection redundant; implementations kept in window.js
- **[09:47]** Added drag handles at linear-region boundaries — full-height hit area + visible grip dots; live preview updates circles + overlay rect on drag (no full re-render); onWindowChange callback triggers re-render on dragend with manualXLo/manualXHi stored in index.js; slider input clears manual overrides
- **[09:52]** Added pan interaction on linear region overlay — box moves in pixel space, xScale.invert maps new pixel positions back to domain so "different portion of data" is revealed without changing window width; dots remain hoverable because overlay is .lower()'d below them in SVG DOM order
- **[09:55]** Fixed tooltip access in linear region — pan overlay was appended after dots (higher SVG z-order) blocking pointer events; moved pan drag to the existing overlay rect which was already .lower()'d below dots
- **[10:01]** Made chart full-page — hid linear/log comparison charts with display:none; adaptive chart gets flex:1 + min-height:0 to fill remaining viewport; SVG uses width:100%/height:100% instead of height:auto to actually fill flex container
- **[10:05]** Added clipPadding option to base-chart — expands clipPath by dot radius on all sides so edge dots render fully; exposed as an API option per Mike Bostock idiom (caller decides, chart doesn't assume)
- **[10:08]** Removed log mode toggle — always use piecewise scale; detectScaleType still runs but mode is overridden to 'piecewise' in renderExperimental
- **[10:15]** Fixed qLo/qHi drag-handle overrides not persisting across re-renders — dragend callback now passes pixel fractions (finalR1/innerW, currentR2/innerW) into onWindowChange; index.js stores manualQLo/QHi and passes them back as qLoOverride/qHiOverride on next render
- **[10:22]** Fixed slider snapping linear window to center after pan — slider now only updates qLo/qHi (pixel fractions), leaving domain values untouched; the panned range stays fixed while slider controls pixel budget
- **[10:45]** Config consolidation: pulled all magic numbers (dot color, radius, opacity, axis colors, tail tick count, overlay/tick colors) out of chart functions into a single options object; makeFmt deduplicated and exported from base-chart.js
- **[10:47]** Added dark/light theme system — CSS custom properties on :root/:data-theme drive page chrome; THEMES.dark/light objects in index.js drive SVG chart colors; toggle button re-renders chart with new theme
- **[10:48]** Added Chart Settings panel — dot size (auto/2px/3px/5px), opacity slider (auto → explicit), tail lines count slider (0–12); all values live-update on change
- **[11:15]** Added scale legibility features on experiment/scale-legibility: faint italic "power"/"linear" region labels at chart top; on-drag dimension annotation (← $8.5M → with arrowheads) inside linear section; "1px ≈ $X" local scale rate in hover tooltip using xScale.invert(px+1) - xScale.invert(px); chartBg option for text halo
- **[11:35]** Merged region labels + span annotation into unified three-region dimension annotations: always-visible dimension lines spanning each scale region with arrowheads, data range value, and scale-type label; replaces the separate static text labels and drag-only span; all three update live during drag
- **[10:27]** Chose approach 3: diagonal hatch lines at equal domain-interval steps, same compression encoding as bunched lines (approach 2) but at 45° — visually reads as texture density rather than ruler marks.
- **[10:27]** Per-tail SVG clipPath elements added (updated dynamically during drag) so 45° lines don't bleed from log tails into the linear region.
- **[10:27]** Fallback flat tint (0.10 opacity) added for when entire tail is narrower than one window-width — ensures tails always have visual weight even before hatch lines appear.
- **[10:28]** Panning window to a narrow range ($5k–$9.7k) reveals the full hatch narrative: sparse lines near boundary → progressive bunching → solid fill at extreme. Compression is legible without any numbers.
- **[11:05]** Fixed hatch gap artifact: Chrome Blink clips SVG <pattern> tile content at tile boundaries even with overflow:visible, causing anti-aliased dots at every tile seam — replaced patterns with explicit full-height <line> elements per band, each clipped by a per-band <clipPath>; no tile boundaries means no seam artifacts.
- **[11:25]** Fixed left tail hatch density ordering: collectBands starting at curXLo found only sub-pixel bands (log scale is maximally compressed at the domain maximum) — rewrote as collectLeftBands iterating from leftMin outward (wide bands first), reversed for boundary-first ordering, boundary gap filled solid; spacing formula inverted to minBandW/bandW so boundary-nearest band is always sparsest.
- **[11:40]** Replaced width-based density (minBandW/bandW vs bandW/maxBandW) with index-based formula BASE_SPACING*(N-i)/N — eliminates the large solid fill on the left tail (extreme-nearest wide band had 0.49px spacing → solid) and makes both tails fully symmetric; spacing is now a pure function of position, not pixel width.
- **[12:30]** Fixed thick solid fill at extreme end of left tail: LINE_MIN_PX=2 was calibrated for width-based formula; with index-based formula, (N-i)/N guarantees the last band triggers solid fill for any N≥5 — meaning 80% of the left tail (one huge 238px band) was solid-filled. Lowered to LINE_MIN_PX=1 (physical visibility threshold at the chart's scale factor); 1.6px SVG spacing ≈ 9px on screen, clearly renders as distinct diagonal lines.
- **[13:15]** Eliminated the boundary gap white band on the left tail: collectLeftBands steps from leftMin inward and stops one partial domain step short of curXLo, leaving a [51.96, 56.75] SVG uncovered zone that was solid-filled at 0.50 opacity (~61px on screen). Fix: extend bands[0] to reach tailX+tailW before the band loop — sparse hatch now runs flush to the left handle, matching how collectBands starts exactly at curXHi for the right tail. No solid fill drawn; right tail's extreme remnant fill is the only 0.50-opacity rect remaining.
- **[13:50]** Root cause found for all the left/right hatch divergence: commit 0cbede2 (Jun 20) silently swapped both tail sub-scales from scalePow(0.3) to scaleLog, bundled into a ruler-ticks/annotations commit with no DEVLOG entry. But scalePow(0.3) was never a mirror either — exponent<1 expands the small end exactly like log. A plain log/pow tail CANNOT mirror its opposite because log measures ratio, which is unbounded going up but floors at zero going down; its slope 1/x always expands the small extreme. All the collectLeftBands / index-formula / gap-fill patches were the hatch layer fighting a scale that was never symmetric.
- **[14:00]** Replaced both tail sub-scales with d3.scaleSymlog fed DISTANCE OUTWARD from the linear-window boundary ((x−xHi) right, (xLo−x) left). Distance is symmetric (no zero-floor), so symlog compresses both tails identically — small-value outliers bunch at the left edge exactly as large-value outliers bunch at the right. This is the windowed generalisation of symlog (linear middle via the existing drag-handle window, log tails). Both tails now run one shared `symlogTail()`; deleted collectLeftBands, the index formula, the boundary-gap fill, and the logMin clamp (symlog handles $0/negatives natively). drag-invert proxies switched scaleLog→scaleSymlog too — stable on USGS (1→251M), no exponential blowup.
- **[14:05]** Chose a span-relative symlog constant (span/κ, κ=12, identical both tails) over slope-matching the constant to the window. Slope-matching was kink-free but tied each tail's compression to its own data span, so the wider tail (NYC right $156M vs left $4.54M) compressed far harder — not a visual mirror. Fixed κ gives both tails one compression profile: pixel-identical hatch bands ([24.31, 11.3, 7.44] both sides on NYC and USGS) regardless of dollar range. Trade-off: a slope kink at each boundary, which actually reinforces the visible log|linear|log region distinction the annotations already label.
- **[14:20]** Reverted the fixed-κ constant — it was wrong. User clarified the goal is faithful inspection of extreme data, NOT a forced visual mirror: "I don't want to massage how that works depending on the data… my ultimate goal is not a visual outcome." Fixed κ normalised away the data (pixel-identical tails regardless of range) = exactly the massaging to avoid. Restored the slope-matched constant: same law both tails (boundary slope = window slope), but the constant is *derived* from each tail's own data+geometry, so output follows the data — NYC now renders honestly asymmetric (left near-linear 5 even bands as its $4.54M range barely needs compression; right one band then solid as its $156M range crams hard). True symmetry now emerges only when the data is symmetric (equal range either side of a centred window), never imposed.
- **[14:40]** Replaced the discrete per-band hatch with a continuous scale-driven fill. User feedback: a near-linear tail (range ≈ linear window) still showed ~5 separate hatch bands, "inferring five different logs, which is obviously incorrect" — the fixed N-band model invents structure a single continuous symlog doesn't have. New fillTailHatch places diagonal lines by the sub-scale itself: local spacing = BASE_SPACING × (local px-per-$ ÷ boundary px-per-$), walked across the tail. Near-linear tail → uniform lines; compressed tail → continuous bunching to solid below LINE_MIN_PX. No bands, no per-band clipPaths, no seams. Deleted collectBands, drawTailBands, BAND_MIN_PX, TAIL_BANDS. Verified continuous (perBandClips=0) on NYC and panned USGS; 124 tests pass.
- **[14:42]** Branch-structure finding + plan: main is a 16-line stub (plain scaleLinear); the entire piecewise/window/scale system was built on the experiment lineage and never merged. All four viz branches descend from shared base e8a5335 (buggy scaleLog tails). The symlog scale is therefore a foundational fix for all of them, not a hatch-branch change. Chosen plan (user): SETTLE the hatch + scale here first, THEN isolate the symlog scale into a clean commit, promote the shared foundation to main (replacing the stub), and rebase the four viz branches onto it. Hatch stays branch-specific.
- **[16:00]** New branch exp/log-rate-arrows: replace the hatch entirely with a "tail ruler" annotation. The linear window's dollar width W is the unit; each log tail is tiled into W-chunks, each = 1× the linear distance. A full-height post marks every chunk boundary (the same vertical line the linear section has at its edge); a chunk wide enough also gets a ←→ arrow, and one wide enough for text gets a stacked "log / ×1" label (matching the linear annotation's type-over-value layout). Each log region is tinted a touch lighter than the linear section so they read as distinct.
- **[16:05]** Density cap is the codebase's canonical tail-tick pattern: walk W-chunks outward, break as soon as a chunk is < RULER_MIN_PX (2px) — monotonic symlog compression guarantees every later chunk is narrower, so this bounds posts at ~(tail width / 2px), never thousands. Past the point where individual arrows no longer fit, the remaining chunks collapse into ONE arrow labelled "log ×N" (N from the scale: remaining dollars ÷ W) — e.g. ×17, ×1.5M, or fractional ×0.5 when a whole tail is smaller than the window. Reuses the overflow-indicator / broken-axis convention.
- **[16:20]** Live x-axis on drag: xScale now reads mutable window state (currentXLo/Hi/R1/R2 + reassignable sub-scales) instead of frozen initial consts, so applyState updates that state and re-calls axisBottom(xScale) on the .x-axis group each frame. Verified ticks track the pan ($4.54M…$113M → $9.05M…$169M). y-axis stays put (correct — x-pan doesn't change the price domain).
- **[16:25]** Edge auto-scroll on pan: when the pointer is held past a chart edge, a d3.timer drives continuous data traversal proportional to overshoot (capped 120px, gain 0.12); accumulated scroll folds into the domain so dragging back is seamless.
- **[16:35]** Replaced per-region tick generation with a stable tickPool (log + linear over [xMin,xMax], computed once); priority-based de-confliction (10ᵏ > 5·10ᵏ > 2·10ᵏ > integer mantissa > rest) ranks by value not position — so labels never re-round or flicker during pan.
- **[16:45]** Fixed handle jump on grab-after-pan: applyLeftDrag/applyRightDrag now invert through xScale.invert(px) (the real slope-matched piecewise scale) instead of a standalone scaleSymlog proxy — eliminating the domain mismatch that caused a visible snap whenever a handle was grabbed mid-pan.
- **[17:15]** Collapsed zone tint: replaced fill-rect with dense post lines at RULER_MIN_PX (2px) spacing — same style and density as the breakpoint threshold, giving the collapsed region the same visual texture as the maximally-compressed end of the individual chunk zone. No fill rect needed; the line density communicates compression directly.
- **[22:00]** Removed settings panel (dot size, opacity, tail lines controls) — these were fine-tuning knobs that cluttered the UI without adding value; defaults work well across all datasets so user-facing configurability isn't needed.
- **[22:00]** Tightness slider feedback loop: onWindowChange now syncs alphaSlider.value and the numeric readout to reflect the actual window width (qHi-qLo) after a handle drag — so slider and chart handles stay in agreement regardless of which one the user adjusts.
- **[10:54]** Pre-Observable review: kept the modern options-object chart factory (`fn(data,{opts})→svg.node()`) over converting to the 2012 "Towards Reusable Charts" getter/setter closure pattern — the closure idiom is superseded in current Observable / d3-gallery notebooks, so converting would move *away* from the Bostock-review bar. Refined the existing factory instead (shrunk options surface to geometry+behaviour).
- **[10:54]** Moved all chart colour out of JS into CSS custom properties. JS now owns only geometry and data-driven opacity (dot density ramp, ruler tint); every fill/stroke comes from `--tokens` (defined per `data-theme` in index.html) via a `CHART_CSS` stylesheet injected into each SVG (fallbacks = dark theme, so charts stay self-contained for Observable). Deleted the JS `THEMES` object; theme toggle is now a live CSS reskin with no chart rebuild.
- **[10:54]** Scale packaging audit: pruned dead `scale.allocAlpha` no-op and the unused `src/scale/ticks.js` (generateTicks); trimmed the CLAUDE.md scale contract to the implemented breakpoint methods (iqr/quantile/percentile/log-iqr). No scale-math changes — 64 tests stay green.
- **[15:25]** Fixed the "stuck linear" one-way trap: widening the window to fully linear then dragging the handles back stranded the window in the outliers, because handle drags inverted pixel→dollar through the *live* adaptive scale — which, once linear, crushes the cluster into a few pixels and can't address it. Switched handle drags to invert through a FIXED log reference scale (xRef), anchored to the current boundary each frame so the increments telescope: no grab-snap, and dragging back over the same pixels lands on the same dollars (verified reversible to the dollar). This is the d3 "preserve the reference, derive the view" idiom (cf. transform.rescaleX).
- **[15:25]** Added double-click-to-reset (canonical d3-zoom/d3-brush gesture) — snaps back to the default centred quantile window at slider 0.5. Required a no-movement guard on pan/handle drags so a pure click no longer re-renders (which would replace the SVG between the two clicks and swallow the gesture).
- **[17:29]** Fixed the "log ×0.0" degenerate region: dragging the linear window's edge to a data extreme left a near-zero-dollar tail (e.g. $10–$10.2) that the pixel allocation still inflated into a half-screen log strip. Scale now treats a tail spanning < 1e-7 of the data range as absent (no region, no pixels even under an explicit override — the linear region extends to the edge); and a real tail narrower than one window-width now shows its dollar span (e.g. "log $70.9k") instead of a meaningless ×0.x multiplier.
- **[18:10]** Reverted the reference-scale ("xRef") drag math: it put a coordinate transform in the chart and had a unit mismatch (screen-pixels added into log-reference-pixel space), so a narrow high-value window collapsed $421k → $0.18 on a one-pixel drag. Handle drags now read the boundary straight off the scale's own invert() — pure math layer, smooth and monotonic. Principle restored: the scale owns the maths, the chart renders what it's given.
- **[18:10]** Added a window cap in the scale so it can never swallow the whole range and flatten the cluster: on multi-decade data the linear window always leaves a log-tail (5% of the log span) on each side; compact data is left uncapped so the scale still degrades to linear. A 2% minimum pixel sliver per tail prevents a capped tail from rendering as a zero-width "log ×0.0" strip. applyState now reads the (possibly capped) window back from the scale, and the chart reports it so the readout matches what's drawn. Replaced the old reachability hack — escaping the (now unreachable) flattened view is moot, and double-click still resets.
- **[18:22]** Fixed window collapse when panning into the cap ("$73.4M – $73.4M, linear $0"): the cap clamped the two window edges independently, so a window panned entirely past the ceiling had both edges snapped onto it → zero width. The scale now shifts the window as a UNIT to fit within [floor, ceil] (preserving width), and exposes scale.windowBounds() so the pan clamps and edge auto-scroll respect the cap instead of the raw data extremes. Panning up now parks the window at [ceil − width, ceil] with its width intact.
- **[18:40]** Code-review fixes. (1) The 2% tail pixel-reserve clamped each edge independently, so on charts wider than ~1000px (minTail > MIN_WINDOW_PX) a narrow window with both tails docked at an edge pushed p1 past p2 — a negative-width linear region that broke the monotonicity invariant. Now the linear region is fitted INTO the post-reserve band [loBound, hiBound], preserving its pixel width and shifting inward to make room, so the reserves can never cross. (2) The arrow-key pan still clamped the domain to xMin/xMax; brought it in line with the mouse pan (clamp to windowBounds(), unit-shift). (3) Added scale tests for the reserve inversion (at 1200px, which the 800px suite missed), the swallow cap, the unit-shift width preservation, and windowBounds() — 64 → 68 tests.
- **[10:51]** Added y-only forceSimulation jitter to base-chart.js: fx freezes x (scale axis exact), forceCollide(r+1) separates overlapping dots, forceY restores to true position — displacement stays ≤ 1-2 radii so the jitter is honest
- **[11:19]** Added jitter toggle button to toolbar: in-place animation via setJitter() method attached to SVG node — easeBackOut on spread (springy), easeCubicInOut on collapse; no full re-render on toggle
- **[13:29]** Replaced jitter button with real CSS toggle switch; added entrance animation (dots settle from true positions on data load); added loading spinner via ::after on chart container
- **[13:36]** Renamed 'Jitter' to 'Spread' — force-directed separation is not random noise
- **[13:36]** Density-adaptive restoring force: per-node neighbour count (within 4r) drives forceY.strength; crowded dots get strength→0.05, isolated dots stay at 0.35 — USGS shallow cluster separates visibly
- **[13:36]** Generation counter in renderExperimental prevents stale entrance animations from a prior dataset load firing on the new chart
- **[13:47]** Added wall force to jitter simulation — constant-strength boundary force prevents dots from escaping chart edges on spread
- **[18:05]** Added cross-protocol NFT lending dataset (1987-row Dune Analytics sample, NFTfi/Blur Blend/Arcade 2023-2025): wired into loader pattern as default dataset, y-axis is loan year, added `yTicks` option to `createChart` to eliminate duplicate integer labels on discrete y domains.
- **[18:18]** Changed NFT lending Y axis from loan year to APR: re-queried Dune with per-protocol APR formulas (NFTfi: (maxRepayment-principal)/principal*(365d/duration); Blur Blend: rate/100 bps; Arcade V3: proratedInterestRate/1e22*(365d/duration)). APR range 1-1000%, median 23.8% — the outlier-APR structure mirrors the outlier-principal structure on X.
- **[14:20]** Found NFTfi CryptoPunks 721 wrapper contract (0x000000000000003607fce1ac9e043a86675c5c2f) via large-loan discovery query; 39 loans 2025-2026 ($32k-$210k) added to CSV bringing total to 4,605 rows
- **[14:22]** $2.8M USDC loan on punk #8348 was on Gondi V3.1 (not NFTfi) — confirmed via gondi.xyz URL; Gondi V3.1 table is `multisourceloan_call_emitloan`, contract 0xf41b389e0c1950dc0b16c9498eae77131cc08a56
- **[14:45]** APR extraction from Gondi output_1 required `aprBps[^0-9]*([0-9]+)` regex — the tranche field is a doubly-escaped stringified JSON array, so `"aprBps":` literal matching always returned NULL
- **[14:50]** Added 1,548 Gondi CryptoPunks loans (V2+V3+V3.1, 2024-2026) to CSV; max outlier $2.75M on punk #8348 — CSV now 6,153 rows
- **[16:54]** Edge gap fix: the minTail (2% ≈ 26px) reserve now COLLAPSES a sub-threshold tail to flush on an interactive drag/pan (no more dead-zone-then-snap when dragging the linear section to a data extreme), while keeping the floor on the auto/slider window so the widest slider setting never swallows the outliers. Gated on isOverride; 69 tests green.

- **[21:09]** Restored the windowCap inset (5% of log span from each extreme), applied to interaction not just the auto window — removing it (to rely on the reset button instead) was the root cause of the pan/handle "snap to the extreme": once the linear window could reach an edge, a tail vanished and the boundary jumped. Capping the reach makes the snap structurally impossible. Chose the guardrail over animating the collapse.
