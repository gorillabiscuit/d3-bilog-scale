/**
 * Render a scatterplot using a log scale on the X axis.
 * Illustrates why log alone fails for two-sided outlier distributions.
 * @param {Element} container
 * @param {Array<{ltv: number, apr: number}>} data
 * @param {object} options
 */
export function createLogChart(container, data, options = {}) {
  // TODO: implement log chart rendering
}
