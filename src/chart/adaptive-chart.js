/**
 * Render a scatterplot using the adaptive piecewise scale on the X axis.
 * @param {Element} container
 * @param {Array<{ltv: number, apr: number}>} data
 * @param {object} options
 */
export function createAdaptiveChart(container, data, options = {}) {
  // TODO: implement adaptive chart rendering
}
