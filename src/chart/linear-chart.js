/**
 * Render a scatterplot using a naive linear scale on the X axis.
 * Illustrates the "crushed middle" problem with outliers.
 * @param {Element} container
 * @param {Array<{ltv: number, apr: number}>} data
 * @param {object} options
 */
export function createLinearChart(container, data, options = {}) {
  // TODO: implement linear chart rendering
}
